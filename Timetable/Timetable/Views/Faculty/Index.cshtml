@model IEnumerable<TimetableApp.Web.Models.FacultyViewModel>
@{
    ViewData["Title"] = "Index";
}
<body style=" background-color: #E6F0FA;">
    <div class="container">
        <div class="table-wrapper">
            <div class="table-title">
                <div>
                    <form asp-controller="Faculty" asp-action="Index" method="post">
                        <p>
                            Навзвание: <input type="text" name="searchString" />
                            <input type="submit" value="Поиск" class="searchButton" />
                        </p>
                    </form>
                    <div>
                        <a asp-controller="Faculty" asp-action="Create" class="btn btn-outline-primary">
                            <span>Добавить запись</span>
                        </a>
                    </div>
                    <br>
                </div>
            </div>
            <table class="table table-bordered" style="background-color: white; box-shadow: 10px white;">
                <thead class="thead-light">
                    <tr>

                        <th>
                            @Html.DisplayNameFor(model => model.FacultyName)
                        </th>
                        <th>Действия</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>

                            <td>
                                @Html.DisplayFor(model => item.FacultyName)
                            </td>
                            <td>
                                <a asp-action="Update" asp-controller="Faculty" asp-route-id="@item.FacultyID" class="editLabel btn btn-outline-primary">Редактировать</a>
                                <a asp-action="Delete" asp-controller="Faculty" asp-route-id="@item.FacultyID" class="deleteLabel btn btn-outline-danger">Удалить</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    </body>

