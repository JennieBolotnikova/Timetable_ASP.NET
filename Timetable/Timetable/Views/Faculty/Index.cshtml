@model IEnumerable<TimetableApp.Web.Models.FacultyViewModel>
@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <div class="table-wrapper">
        <div class="table-title">
            <div>
                <form asp-controller="Faculty" asp-action="Index" method="post">
                    <p>
                        Навзвание: <input type="text" name="searchString" />
                        <input type="submit" value="Поиск" class="searchButton" />
                    </p>
                </form>
                <div>
                    <a asp-controller="Faculty" asp-action="Create" class="btn createButton">
                        <span>Добавить запись</span>
                    </a>
                </div>
            </div>
        </div>
        <table class="table table-hover">
            <thead>
                <tr>
                    
                    <th>
                        @Html.DisplayNameFor(model => model.FacultyName)
                    </th>
                    <th>Действия</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        
                        <td>
                            @Html.DisplayFor(model => item.FacultyName)
                        </td>
                        <td>
                            <a asp-action="Update" asp-controller="Faculty" asp-route-id="@item.FacultyID" class="editLabel">Редактирование</a>
                            <a asp-action="Delete" asp-controller="Faculty" asp-route-id="@item.FacultyID" class="deleteLabel">Удаление</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

