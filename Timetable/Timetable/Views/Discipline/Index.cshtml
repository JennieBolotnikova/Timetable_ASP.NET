@model IEnumerable<TimetableApp.Web.Models.DisciplineViewModel>
@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <div class="table-wrapper">
        <div class="table-title">
            <div>
                <form asp-controller="Discipline" asp-action="Index" method="post">
                    <p>
                        <input type="text" name="search" />
                        <input type="submit" value="Поиск" class="searchButton" />
                    </p>
                </form>
                <div>
                    <a asp-controller="Discipline" asp-action="Create" class="btn createButton">
                        <span>Добавить запись</span>
                    </a>
                </div>
            </div>
        </div>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.DisciplineID)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.DisciplineNmae)
                    </th>
                    <th>Действия</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(model => item.DisciplineID)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.DisciplineNmae)
                        </td>
                        <td>
                            <a asp-action="Update" asp-controller="Discipline" asp-route-id="@item.DisciplineID" class="editLabel">Редактирование</a>
                            <a asp-action="Delete" asp-controller="Discipline" asp-route-id="@item.DisciplineID" class="deleteLabel">Удаление</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
