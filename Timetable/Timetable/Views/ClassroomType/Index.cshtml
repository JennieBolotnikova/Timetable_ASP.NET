@model IEnumerable<TimetableApp.Web.Models.ClassroomTypeViewModel>
@{
    ViewData["Title"] = "Index";
}
<body style=" background-color: #E6F0FA;">
    <div class="container">
        <div class="table-wrapper">
            <div class="table-title">
                <div>
                    <form asp-controller="ClassroomType" asp-action="Index" method="post">
                    </form>
                    <div>
                        <a asp-controller="ClassroomType" asp-action="Create" class="btn btn-outline-primary">
                            <span>Добавить запись</span>
                        </a>
                    </div>
                    <br>
                </div>
            </div>
            <table class="table table-bordered" style="background-color: white; box-shadow: 10px white;">
                <thead class="thead-light">
                    <tr>

                        <th>
                            @foreach (var item in Model)
                            {
                                @if (item == Model.First())
                                {
                                    @Html.DisplayNameFor(model => item.СlassroomTypeName)

                                }
                            }
                        </th>
                        <th>Действия</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>

                            <td>
                                @Html.DisplayFor(model => item.СlassroomTypeName)
                            </td>
                            <td>
                                <a asp-action="Update" asp-controller="СlassroomType" asp-route-id="@item.ClassroomTypeID" class="editLabel btn btn-outline-primary">Редактировать</a>
                                <a asp-action="Delete" asp-controller="СlassroomType" asp-route-id="@item.ClassroomTypeID" class="deleteLabel btn btn-outline-danger">Удалить</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</body>
