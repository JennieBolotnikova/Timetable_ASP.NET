@model TimetableApp.Web.Models.BellViewModel
@{
    ViewData["Title"] = "Создание";
}
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="LessonStartTime" class="control-label"></label>
                <input asp-for="LessonStartTime" class="form-control" />
                <span asp-validation-for="LessonStartTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="LessonEndTime" class="control-label"></label>
                <input asp-for="LessonEndTime" class="form-control" />
                <span asp-validation-for="LessonEndTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Добавить" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<div>
    <a asp-action="Index">Назад</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
