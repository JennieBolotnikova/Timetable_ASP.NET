@model IEnumerable<TimetableApp.Web.Models.BellViewModel>
@{
    ViewData["Title"] = "Index";
}
<body style=" background-color: #E6F0FA;">
    <div class="container">
        <div class="table-wrapper">
            <div class="table-title">
                <div>
                    <div>
                        <a asp-controller="Bell" asp-action="Create" class="btn btn-outline-primary" style=" background-color: white;">
                            <span>Добавить запись</span>
                        </a>
                    </div>
                    <br>
                </div>
            </div>
            <table class="table table-bordered" style="background-color: white; box-shadow: 10px white;">
                <thead class="thead-light">
                    <tr>

                        <th>
                            @Html.DisplayNameFor(model => model.LessonStartTime)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.LessonEndTime)
                        </th>
                        <th>Действия</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>

                            <td>
                                @Html.DisplayFor(model => item.LessonStartTime)
                            </td>
                            <td>
                                @Html.DisplayFor(model => item.LessonEndTime)
                            </td>
                            <td>
                                <a asp-action="Update" asp-controller="Bell" asp-route-id="@item.BellID" class="editLabel btn btn-outline-primary">Редактировать</a>
                                <a asp-action="Delete" asp-controller="Bell" asp-route-id="@item.BellID" class="deleteLabel btn btn-outline-danger">Удалить</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</body>
