@model IEnumerable<TimetableApp.Web.Models.ActivityTypeViewModel>
@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <div class="table-wrapper">
        <div class="table-title">
            <div>
                <form asp-controller="ActivityType" asp-action="Index" method="post">
                    <p>
                        <input type="text" name="search" />
                        <input type="submit" value="Поиск" class="searchButton" />
                    </p>
                </form>
                <div>
                    <a asp-controller="ActivityType" asp-action="Create" class="btn createButton">
                        <span>Добавить запись</span>
                    </a>
                </div>
            </div>
        </div>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.ActivityTypeID)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ActivityTypeName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ActivityTypesShortName)
                    </th>
                    <th>Действия</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    <td>
                        @Html.DisplayFor(model => item.ActivityTypeID)
                    </td>
                    <td>
                        @Html.DisplayFor(model => item.ActivityTypeName)
                    </td>
                    <td>
                        @Html.DisplayFor(model => item.ActivityTypesShortName)
                    </td>
                    <td>
                        <a asp-action="Update" asp-controller="ActivityType" asp-route-id="@item.ActivityTypeID" class="editLabel">Редактирование</a>
                        <a asp-action="Delete" asp-controller="ActivityType" asp-route-id="@item.ActivityTypeID" class="deleteLabel">Удаление</a>
                    
                    </td>
                </tr>
                }
            </tbody>
        </table>
    </div>
</div>


