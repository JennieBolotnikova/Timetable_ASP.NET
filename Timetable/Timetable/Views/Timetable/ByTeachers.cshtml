@model TimetableApp.Web.Models.PaginatedList<TimetableApp.Web.Models.TimetableViewModel>

@{
    ViewData["Title"] = "ByTeachers";
}

<div class="container">
    <div class="table-wrapper">
        <div class="table-title">
            <div>
                <form asp-controller="Timetable" asp-action="ByTeachers" method="post">
                    <p>
                        ФИО преподавателя: <input type="text" name="searchString" />
                        <input type="submit" value="Поиск" class="searchButton" />
                    </p>
                 
                    
                </form>
            </div>
        </div>
    </div>
    <table class="table table-hover">
        <thead>
            <tr>
                @foreach (var item in Model)
                {
                    @if (item == Model.First())
                    {

                        <th>
                            @Html.DisplayNameFor(model => item.Teacher)
                        </th>

                        <th>
                            @Html.DisplayNameFor(model => item.Date)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => item.Day)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => item.BellID)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => item.Discipline)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => item.ActivityType)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => item.Group)
                        </th>

                        <th>
                            @Html.DisplayNameFor(model => item.Classroom)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => item.Semester)
                        </th>
                        <th>Действия</th>
                    }

                }


            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr>

                <th>
                    @Html.DisplayFor(model => item.Teacher)
                </th>

                <th>
                    @Html.DisplayFor(model => item.Date)
                </th>
                <th>
                    @Html.DisplayFor(model => item.Day)
                </th>
                <th>
                    @Html.DisplayFor(model => item.BellID)
                </th>
                <th>
                    @Html.DisplayFor(model => item.Discipline)
                </th>
                <th>
                    @Html.DisplayFor(model => item.ActivityType)
                </th>

                <th>
                    @Html.DisplayFor(model => item.Group)
                </th>
                <th>
                    @Html.DisplayFor(model => item.Classroom)
                </th>
                <th>
                    @Html.DisplayFor(model => item.Semester)
                </th>
                <th>
                    <a asp-action="Update" asp-controller="Timetable" asp-route-id="@item.ID" class="editLabel">Редактирование</a>
                    <a asp-action="Delete" asp-controller="Timetable" asp-route-id="@item.ID" class="deleteLabel">Удаление</a>
                </th>
            </tr>

            }
        </tbody>
    </table>
    @{
        var prevDisabled = !Model.HasPreviousPage ? "disabled" : "";
        var nextDisabled = !Model.HasNextPage ? "disabled" : "";
    }
    <a asp-action="Index"
       asp-route-pageNumber="@(Model.PageIndex - 1)"
       class="btn btn-default @prevDisabled">
        Previous
    </a>
    <a asp-action="Index"
       asp-route-pageNumber="@(Model.PageIndex + 1)"
       class="btn btn-default @nextDisabled">
        Next
    </a>
</div>
</div>

