@model IEnumerable<TimetableApp.Web.Models.SemesterViewModel>
@{
    ViewData["Title"] = "Index";
}
<body style=" background-color: #E6F0FA;">
    <div class="container">
        <div class="table-wrapper">
            <div class="table-title">
                <div>
                    <form asp-controller="Semester" asp-action="Index" method="post">
                    </form>
                    <div>
                        <a asp-controller="Faculty" asp-action="Create" class="btn btn-outline-primary">
                            <span>Добавить запись</span>
                        </a>
                    </div>
                    <br>
                </div>
            </div>
            <table class="table table-bordered" style="background-color: white; box-shadow: 10px white;">
                <thead class="thead-light">
                    <tr>

                        <th>
                            @Html.DisplayNameFor(model => model.SemesterNumber)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SemesterTitle)
                        </th>
                        <th>Действия</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(model => item.SemesterNumber)
                            </td>
                            <td>
                                @Html.DisplayFor(model => item.SemesterTitle)
                            </td>
                            <td>
                                <a asp-action="Update" asp-controller="Semester" asp-route-id="@item.SemesterID" class="editLabel btn btn-outline-primary">Редактировать</a>
                                <a asp-action="Delete" asp-controller="Semester" asp-route-id="@item.SemesterID" class="deleteLabel btn btn-outline-danger">Удалить</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</body>

