@model IEnumerable<TimetableApp.Web.Models.SemesterViewModel>
@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <div class="table-wrapper">
        <div class="table-title">
            <div>
                <form asp-controller="Semester" asp-action="Index" method="post">
                   
                </form>
                <div>
                    <a asp-controller="Faculty" asp-action="Create" class="btn createButton">
                        <span>Добавить запись</span>
                    </a>
                </div>
            </div>
        </div>
        <table class="table table-hover">
            <thead>
                <tr>
                   
                    <th>
                        @Html.DisplayNameFor(model => model.SemesterNumber)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.SemesterTitle)
                    </th>
                    <th>Действия</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                   
                    <th>
                        @Html.DisplayFor(model => item.SemesterNumber)
                    </th>
                    <th>
                        @Html.DisplayFor(model => item.SemesterTitle)
                    </th>
                    <th>
                        <a asp-action="Update" asp-controller="Semester" asp-route-id="@item.SemesterID" class="editLabel">Редактирование</a>
                        <a asp-action="Delete" asp-controller="Semester" asp-route-id="@item.SemesterID" class="deleteLabel">Удаление</a>
                    </th>
                </tr>
                }
            </tbody>
        </table>
    </div>
</div>


